spring.application.name=backend
spring.datasource.url=jdbc:mysql://localhost:3306/smart_campus
spring.datasource.username=root
spring.datasource.password=Kabi0522

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

app.admin.email=admin@smartcampus.com
app.admin.password=Admin@123
app.admin.full-name=System Admin
app.admin.it-number=ITADMIN01

spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:kabiththananparan@gmail.com}
spring.mail.password=${MAIL_PASSWORD:gpwerbeyssujvheh}
spring.mail.properties.mail.smtp.auth=${MAIL_SMTP_AUTH:true}
spring.mail.properties.mail.smtp.starttls.enable=${MAIL_SMTP_STARTTLS:true}
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.enable=${MAIL_SMTP_SSL_ENABLE:false}
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
spring.mail.properties.mail.smtp.connectiontimeout=20000
spring.mail.properties.mail.smtp.timeout=20000
spring.mail.properties.mail.smtp.writetimeout=20000

app.mail.from=${MAIL_FROM:${MAIL_USERNAME:no-reply@smartcampus.local}}
app.mail.allow-dev-otp-fallback=${MAIL_ALLOW_DEV_OTP_FALLBACK:true}
